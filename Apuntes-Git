Comandos:

git init: crear un repositorio asociado a una carpeta.

git status -s: listar el contenido del repositorio git
            
            ?? el archivo o carpeta no tiene seguimiento por Git
            A el archivo o carpeta pasa al area de ensayo y que esta seguido por git
            
git add . : pasar el contenido de la carpeta de trabajo al area de ensayo

git commit -m "texto estado proyecto" : para guardar los camios del proyecto

git log -oneline : para ver los commits

git rest --hard numero del commit : para volver a la version indicada
